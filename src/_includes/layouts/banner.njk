---
layout: base
---
<!-- HERO IMAGE AND CTA IMAGE TABLE -->
<table>
	<tr>
		<td style="padding:20px 0;">
			<table role="presentation" cellpadding="0" cellspacing="0" border="0" width="600" class="emailwrapto100pc">
				<tbody>
					<tr>
						<td><a href="{% if imageURL %}{{imageURL}}{% else %}{affiliate_website}{% endif %}"><img src="{% if imageSRC %}{{imageSRC}}{% else %}https://media.sailthru.com/64p/1k3/9/r/5d8e7393531ee.jpg{% endif %}" width="600" class="image-resize" border="0" alt="{% if bannerAlt %}{{bannerAlt}}{% else %}Freedom can't protect itself text with the Statue of Liberty's torch.{% endif %}" style="display:block;" /></a></td>
					</tr>
					<!-- REMOVE <tr> BELOW TO ONLY SHOW IMAGE -->
					<tr>
						<td style="text-align:center;background-color:#0055aa;font-family: Arial,'Helvetica Neue',sans-serif;color:#ffffff;font-size:18px;padding:10px;text-transform:uppercase;"><a role="button" href="{% if buttonURL %}{{buttonURL}}{% else %}{affiliate_website}{% endif %}" style="color:#ffffff;font-size:20px;text-decoration: none;text-transform: uppercase;font-weight:900;">{% if buttonText %}{{buttonText}}{% else %}Take Action Now{% endif %}</a></td>
					</tr>
					<!-- REMOVE <tr> BELOW TO ONLY SHOW IMAGE -->
				</tbody>
			</table>
		</td>
	</tr>
	<!-- HERO IMAGE AND CTA IMAGE TABLE -->
</table>

{{ content | safe }}
